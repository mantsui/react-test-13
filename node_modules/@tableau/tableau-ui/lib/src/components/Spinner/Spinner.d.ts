import * as React from 'react';
export interface SpinnerProps {
    /** The class name applied to the component. */
    className?: string;
    /**
     * * `"dark"` sets the spinner color to a dark grey
     * * `"light"` sets the spinner color to a light grey
     * @default '"dark"'
     */
    color?: 'dark' | 'light';
    /**
     * The dimension of the spinner.
     * @default 50
     */
    dimension?: number;
    /** The id applied to the component. */
    id?: string;
    /**
     * When true, applies a semi-transparent underlay.
     *
     * NOTE: When true, the dimension of the component will be 1.3x the passed `dimension`, with the
     * spinner dimensions remaining as the passed `dimension`
     * @default false
     */
    showUnderlay?: boolean;
    /** The style override applied to the component. */
    style?: React.CSSProperties;
}
export declare const DarkColor = "rgb(97, 101, 112)";
export declare const LightColor = "rgba(255, 255, 255, 0.8)";
/**
 * An inline spinner.
 *
 * NOTE: An ancestor of the spinner should have `aria-busy="true"`
 */
export declare const Spinner: React.ForwardRefExoticComponent<SpinnerProps & React.RefAttributes<HTMLDivElement>>;
