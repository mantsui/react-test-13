import * as React from 'react';
import { DivAttrs } from '../../utils/NativeProps';
export interface PillProps extends DivAttrs {
    /**
     * * `"continuous"`: green pill
     * * `"discrete"`: blue pill
     * * `"invalid"`: red pill
     * * `"other"`: gray pill
     */
    kind: 'continuous' | 'discrete' | 'invalid' | 'other';
    /**
     * When false, pills have richer coloring, matching the style of Pills in Tableau Cards and Shelves.
     *
     * When true, pills have lighter coloring, matching the style of Pills in the Tableau Schema Pane.
     */
    schema?: boolean;
    /** When true, coloring is darkened to indicate the Pill is selected. */
    selected?: boolean;
}
/**
 * Props defined in `PillProps` will be used by this component. All other provided props will be passed
 * to the root `<div>` element.
 */
export declare const Pill: React.ForwardRefExoticComponent<PillProps & React.RefAttributes<HTMLDivElement>>;
