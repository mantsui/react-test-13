import * as React from 'react';
import { ButtonAttrs } from '../../utils/NativeProps';
export interface ButtonProps extends ButtonAttrs {
    /**
     * The following current styles should be used depending on the purpose of the button:
     *
     * * `"outline"` should be used for most buttons
     * * `"primary"` should be used for the primary action of a dialog, e.g. "OK"
     * * `"destructive"` should be used for buttons which cause a destructive action, e.g. "Delete"
     *
     * The following prior styles are deprecated:
     *
     * * `"attentionRed"`: instead use `"destructive"`
     * * `"blackText"`: instead use `"outline"`
     * * `"filled"`: instead use `"outline"`
     * * `"filledGreen"`: instead use `"primary"`
     * @default '"outline"'
     */
    kind?: 'outline' | 'primary' | 'destructive' | 'attentionRed' | 'blackText' | 'filled' | 'filledGreen';
}
export declare type ButtonKind = ButtonProps['kind'];
/**
 * Props defined in `ButtonProps` will be used by this component. All other provided props will be passed to the native `<button>` element.
 */
export declare const Button: React.ForwardRefExoticComponent<ButtonProps & React.RefAttributes<HTMLButtonElement>>;
