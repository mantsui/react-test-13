import * as React from 'react';
import { SelectorProps } from '../Selector';
export interface CheckboxProps extends SelectorProps {
    /**
     * When true, applies an indeterminate visual state.
     * This is a purely visual effect, i.e. there is no functional change.
     *
     * NOTE: the behavior of indeterminate checkboxes in Internet Explorer differs from other browsers.
     */
    indeterminate?: boolean;
}
/**
 * Props defined in `CheckboxProps` will be used by this component. All other provided props will be passed to the native `<input>` element.
 *
 * Renders as an inline block element by default. To render as a block element, pass `display: flex` in the `style` prop.
 */
export declare const Checkbox: React.ForwardRefExoticComponent<CheckboxProps & React.RefAttributes<HTMLInputElement>>;
